public class Trap extends  Enemy {

    public Trap(String name, char tile, Health health, int attackPoints, int defencePoints, int visionRange, int experienceValue, int range, int respwan, int visibility ,Position position) {
        super(name, tile, health, attackPoints, defencePoints, visionRange, experienceValue, position);
    }
}
