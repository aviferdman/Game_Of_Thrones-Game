public class Trap extends  Enemy {

    public Trap(Integer experienceValue, char tile, String name, Health health, Integer attackPoints, Integer defencePoints, Position position) {
        super(experienceValue, tile, name, health, attackPoints, defencePoints, position);
    }
}
